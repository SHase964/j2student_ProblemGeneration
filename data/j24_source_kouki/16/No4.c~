#include<stdio.h>
#include<stdlib.h>
#include<time.h>

int main(void)
{
  int i;
  int *num;
  
  scanf("%d", %k);

  num = (int *)malloc(sizeof(int)*i);
  
  srand(unsigned)time(NULL);

  printf("(1)")
  for(a=0; a<i; a++){
    *num = rand()%100;
    printf("%d ", num[i]);
  }

  return 0:

}
