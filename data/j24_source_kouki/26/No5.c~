#include <stdio.h>
#include <stdlib.h>

int main(void){
  int **array;
  int n;

  printf("enter the num : ");
  scanf("%d",&n);

  *array = (int **)malloc(sizeof(int *) * n);
  for(int i = 0;i < n;i++){
    array = (int *)malloc(sizeof(int) * n);
  }
  
