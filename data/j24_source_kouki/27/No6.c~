#include<stdio,h>
#include<stdlib.h>

int main(void){
  char *str1, *str2;
  char **mix;
  int num1,num2;

  printf("input size : ");
  scanf("%d",&num1);
  str1=(char *)malloc(sizeof(char)*(num1+1));
  fgets(str1,num1+1,stdin);


  free(str1);
  return 0;
}
