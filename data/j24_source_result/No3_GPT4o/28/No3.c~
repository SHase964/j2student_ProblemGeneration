#include<stdio.h>
#include<stdlib.h>

int main(){

  int num;
  int i;
  int *pt;

  printf("何個整数を入力する?=");
  scanf("%d",&num);

  pt[i] = (int *)malloc(sizeof(int) *(num+1));

  for(i=0; i<num; i++){
    scanf("%d",pt[num-i])
      }

  free(pt[i]);
  return 0;
}
