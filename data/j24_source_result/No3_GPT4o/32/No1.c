#include <sudio.h>
#include <stdlib.h>

#define NUM 3

int main(void) {
  int *number;
  int i;

  number = (int *)malloc(sizeof(int)*(3+1));
  
  for(i=0; i<3; i++) {
    number[i] = 
  }

  free(number);
  return 0;
}
