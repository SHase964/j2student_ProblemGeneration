#include <stdio.h>
#include <stdlib.h>

int * word_count(char *array){

  array = (char *)malloc(sizeof(char));
}

int main(void){

  char *array;
  int *ans;

  printf("文字列を入力してください。\n");
  fget();

  ans = word_count(array);

  return 0;
}
