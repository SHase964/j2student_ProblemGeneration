#include<stdio.h>
#include<stdlib.h>

int main(void){
  int *i;
  int k;
  int a;

  scanf("%d", &k);
  
  i = (int *)malloc(sizeof(int) * k);

  for(a=1; a<=i; a++){
    scanf("%d", i[]);
  }

  return 0;

  free(i);
}
