#include<stdio.h>
#include<stdlib.h>

int main(void)
{
  int i;
  int *pt;
  int mozi;
  int gyaku;
  char str[mozi];
  scanf("%d",&mozi);

  pt=(int* )malloc(sizeof(int)*mozi);



  for(i=count-1;i>=0;i--){
    printf("gyaku=%c\n",gyaku);
  }
  return 0;
}
